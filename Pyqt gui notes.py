# Comes with a lot of styles and widgets already built in.
#pyqt is better for larger apps than tkinter because it has so much already built in. 
import sys
import PyQt4
import  QtGui
app = QtGui.QApplication(sys.argv)

#Important to look at the letter case. not normal
